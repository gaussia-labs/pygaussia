"""The category search and the arithmetic it rests on.

Nothing is re-exported here: ``scoring`` imports no model and no third-party package, and a
facade that pulled a search implementation would drag an embedder into every import of it
(FR-037).
"""
